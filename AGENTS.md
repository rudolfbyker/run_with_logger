## Environment

- Use the `venv`.
- Add `src` to `PYTHONPATH`.

## Code formatting

- Run `black .`.

## Type checking

- Requirements are in `mypy-requirements.txt`.
- Run `mypy .`.

## Unit testing

- Requirements are in `test-requirements.txt`.
- Run `python -m unittest`.
